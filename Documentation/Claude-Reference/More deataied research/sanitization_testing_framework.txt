# Content Sanitization Testing Framework

# and Integration Guide

**Author** : Manus AI

**Date** : June 18, 2025

**Project** : FileSorter Content Sanitization Bypass - Testing and Integration

**Context** : QiuYannnn Methodology + Apple Foundation Models + Content Safety

## Executive Summary

This document provides a comprehensive testing framework and integration guide for

the content sanitization bypass system in FileSorter. The testing framework ensures that

legitimate technical content, particularly audio equipment filenames like

"F_SEEL_SEX1S.wav", can be processed reliably while maintaining appropriate safety

measures. The integration guide demonstrates how to incorporate the sanitization

bypass system into the existing QiuYannnn methodology implementation.

## Testing Framework Architecture

### Comprehensive Test Dataset

The testing framework relies on a carefully curated dataset that represents real-world

scenarios where legitimate technical content might trigger content sanitization systems.

This dataset serves as the foundation for validating the effectiveness of the bypass

system.

**Audio Equipment Test Cases:**

The core test dataset includes legitimate audio equipment filenames that are known to

trigger false positives in content sanitization systems:

```
let audioEquipmentTestCases = [
// sE Electronics products (SEX series triggers false
positives)
"F_SEEL_SEX1S.wav",
"M_SEEL_SEX1S.wav",
"F_SEEL_SEX2A.wav",
"M_SEEL_SEX2A.wav",
```

```
// Other potentially problematic equipment names
"F_Coles_4038_Ribbon.wav",
"M_RCA_44BX_Vintage.wav",
"F_Electro_Voice_RE20.wav",
"M_Audio_Technica_AT4040.wav",
```
```
// Technical specifications that might trigger filters
"Vocal_Sample_96kHz_24bit_Stereo.wav",
"Drum_Kit_Close_Mic_192kHz.wav",
"Guitar_Amp_Room_Tone_48kHz.wav",
```
```
// Professional naming conventions
"F_Lead_Vocal_Take_01_Comp.wav",
"M_Backing_Vocal_Harmony_V2.wav",
"Drum_Overhead_L_Final_Mix.wav"
]
```
**Edge Case Test Scenarios:**

Beyond standard equipment names, the test framework includes edge cases that

challenge the bypass system:

```
let edgeCaseTestScenarios = [
// Multiple potential triggers in single filename
"F_SEEL_SEX1S_Close_Mic_Take_69.wav",
```
```
// Mixed technical and potentially problematic content
"Studio_Session_XXX_Equipment_Test.wav",
```
```
// International equipment names with special characters
"Röde_NTK_Röhrenmic_Test.wav",
```
```
// Version numbers that might trigger filters
"Microphone_Comparison_v6.9.wav",
```
```
// Technical abbreviations
"DI_Box_Test_BSS_AR133.wav"
]
```
### Automated Testing Pipeline

The testing framework implements a comprehensive automated pipeline that validates

the sanitization bypass system across multiple dimensions:

**Unit Testing for Individual Components:**


Each component of the sanitization bypass system is tested independently to ensure

correct functionality:

```
class ContentSanitizationBypassTests : XCTestCase {
```
```
var bypassSystem: ContentSanitizationBypass!
```
```
override func setUp() {
super .setUp()
bypassSystem = ContentSanitizationBypass()
}
```
```
func testAudioEquipmentPatternDetection() async throws {
let testContent = "F_SEEL_SEX1S.wav"
let fileContext = FileContext(
fileName: testContent,
fileExtension: "wav",
directoryPath: "/Users/test/Audio Files 96khz/",
fileSize: 45_000_000,
hasRichMetadata: true ,
audioMetadata: AudioMetadata(duration: 180.0,
sampleRate: 96000 , channels: 2 )
)
```
```
let result = try await
bypassSystem.processContentSafely(
testContent,
fileContext: fileContext,
processingMode: .comprehensive
)
```
```
XCTAssertTrue(result.isSafe, "sE Electronics SEX1S
should be recognized as legitimate equipment")
XCTAssertGreaterThan(result.confidence, 0.8,
"Confidence should be high for known equipment")
```
```
XCTAssertEqual(result.bypassReason, .audioTechnicalPattern,
"Should be bypassed due to audio technical pattern")
}
```
```
func testTechnicalSpecificationRecognition() async throws {
let testContent = "Vocal_Sample_96kHz_24bit_Stereo.wav"
let fileContext = createAudioFileContext(testContent)
```
```
let result = try await
bypassSystem.processContentSafely(
testContent,
fileContext: fileContext
)
```
```
XCTAssertTrue(result.isSafe, "Technical specifications
```

```
should be recognized as legitimate")
```
```
XCTAssertEqual(result.bypassReason, .technicalSpecification)
}
```
```
func testContextualAnalysis() async throws {
let testContent = "Potentially_Problematic_Content.wav"
let audioDirectoryContext = FileContext(
fileName: testContent,
fileExtension: "wav",
directoryPath: "/Users/test/Professional Audio
Samples/Microphones/",
fileSize: 25_000_000,
hasRichMetadata: true ,
audioMetadata: AudioMetadata(duration: 120.0,
sampleRate: 48000 , channels: 1 )
)
```
```
let result = try await
bypassSystem.processContentSafely(
testContent,
fileContext: audioDirectoryContext
)
```
```
XCTAssertTrue(result.isSafe,
"Content in audio directory context should be treated as
legitimate")
}
}
```
**Integration Testing with QiuYannnn Methodology:**

The testing framework validates that the sanitization bypass system integrates

seamlessly with the QiuYannnn per-file processing approach:

```
class QiuYannnIntegrationTests : XCTestCase {
```
```
func testPerFileProcessingWithSanitizationBypass() async
throws {
let testDirectory = createTestAudioDirectory()
let organizer = FileSorterOrganizer(
foundationModels:
MockFoundationModelsManager().getSystemModel(),
mode: .aiIntelligent
)
```
```
var progressUpdates: [(Double, String)] = []
```
```
let result = try await
organizer.organizeDirectory(testDirectory) { progress, status in
```

```
progressUpdates.append((progress, status))
}
```
_// Verify that problematic filenames were processed
successfully_
**let** problematicFiles = result.analysisResults.filter {
$0.fileName.contains("SEX1S") ||
$0.fileName.contains("problematic")
}

XCTAssertFalse(problematicFiles.isEmpty, "Should have
processed files with potentially problematic names")

**for** file **in** problematicFiles {
XCTAssertGreaterThan(file.confidence, 0.6,
"Bypassed files should have reasonable confidence scores")
XCTAssertNotEqual(file.suggestedCategory,
"Uncategorized", "Bypassed files should be properly
categorized")
}
}

**func** testTokenBudgetPreservation() **async throws** {
_// Verify that sanitization bypass doesn't consume
excessive tokens_
**let** tokenEngine = TokenSafeAnalysisEngine(systemModel:
MockSystemLanguageModel())
**let** problematicContent =
"F_SEEL_SEX1S_Close_Mic_Take_69_Final_Mix.wav"

**let** file = FileInventoryItem(
id: UUID(),
fileName: problematicContent,
url: URL(fileURLWithPath: "/test/ **\
(** problematicContent **)** "),
fileSize: 50_000_000,
creationDate: Date(),
modificationDate: Date(),
fileExtension: "wav",
contentType: UTType.wav,
fileTypeCategory: .audio,
isSupported: **true**
)

**let** result = **try await** tokenEngine.analyzeFile(file,
content: "Audio file with technical metadata")

_// Verify that analysis completed successfully despite
potentially problematic filename_
XCTAssertNotNil(result)
XCTAssertGreaterThan(result.confidence, 0.5)


#### }

#### }

**Performance Testing:**

The testing framework includes performance benchmarks to ensure that the sanitization

bypass system doesn't significantly impact processing speed:

```
class PerformanceTests : XCTestCase {
```
```
func testBypassProcessingSpeed() throws {
let bypassSystem = ContentSanitizationBypass()
let testCases = generateLargeTestDataset(count: 1000 )
```
```
measure {
let expectation = XCTestExpectation(description:
"Process all test cases")
```
```
Task {
for testCase in testCases {
let fileContext = createFileContext( for :
testCase)
_ = try await
bypassSystem.processContentSafely(
testCase,
fileContext: fileContext,
processingMode: .quick
)
}
expectation.fulfill()
}
```
```
wait( for : [expectation], timeout: 30.0)
}
}
```
```
func testMemoryUsage() throws {
// Test memory usage during large batch processing
let initialMemory = getMemoryUsage()
```
```
let bypassSystem = ContentSanitizationBypass()
let largeBatch = generateLargeTestDataset(count: 5000 )
```
```
let expectation = XCTestExpectation(description:
"Process large batch")
```
```
Task {
for testCase in largeBatch {
let fileContext = createFileContext( for :
testCase)
```

```
_ = try await bypassSystem.processContentSafely(
testCase,
fileContext: fileContext
)
}
expectation.fulfill()
}
```
```
wait( for : [expectation], timeout: 120.0)
```
```
let finalMemory = getMemoryUsage()
let memoryIncrease = finalMemory - initialMemory
```
```
XCTAssertLessThan(memoryIncrease, 100_000_000, "Memory
usage should not increase by more than 100MB")
}
}
```
### Real-World Validation Testing

Beyond automated testing, the framework includes provisions for real-world validation

with actual user content:

**Professional Audio User Testing:**

The testing framework supports validation with real professional audio content:

```
class RealWorldValidationTests : XCTestCase {
```
```
func testProfessionalAudioDirectory() async throws {
// This test would be run with actual professional audio
directories
// provided by audio engineers and producers
```
```
guard let testDirectory = getRealWorldTestDirectory()
else {
throw XCTSkip("Real-world test directory not
available")
}
```
```
let organizer = FileSorterOrganizer(
foundationModels:
SystemLanguageModel(useCase: .contentTagging),
mode: .aiIntelligent
)
```
```
let result = try await
organizer.organizeDirectory(testDirectory) { _ , _ in }
```

```
// Validate that no legitimate files were blocked
let blockedFiles = result.analysisResults.filter {
$0.confidence < 0.3 }
```
```
for blockedFile in blockedFiles {
print("Potentially incorrectly blocked file: \
( blockedFile.fileName ) ")
// Manual review required for these cases
}
```
```
// Validate organization quality
let wellOrganizedFiles = result.analysisResults.filter
{ $0.confidence > 0.7 }
let organizationQuality =
Double(wellOrganizedFiles.count) /
Double(result.analysisResults.count)
```
```
XCTAssertGreaterThan(organizationQuality, 0.8,
"At least 80% of files should be well-organized")
}
}
```
## Integration Guide

### Step-by-Step Integration Process

The integration of the content sanitization bypass system into the existing FileSorter

implementation follows a systematic approach that ensures compatibility with the

QiuYannnn methodology and Apple Foundation Models framework.

**Step 1: Initialize Sanitization Bypass System**

The first step involves integrating the bypass system into the main FileSorter organizer:

```
// In FileSorterOrganizer.swift
@available(macOS 26.0, *)
class FileSorterOrganizer {
private let foundationModel: SystemLanguageModel?
private let tokenSafeEngine: TokenSafeAnalysisEngine?
private let sanitizationBypass:
ContentSanitizationBypass // Add this
```
```
init (foundationModels: SystemLanguageModel?, mode:
FileSorterAppState.SortingMode) {
self .foundationModel = foundationModels
self .mode = mode
```
```
if let model = foundationModels {
```

```
self .tokenSafeEngine =
TokenSafeAnalysisEngine(systemModel: model)
} else {
self .tokenSafeEngine = nil
}
```
```
// Initialize sanitization bypass system
self .sanitizationBypass = ContentSanitizationBypass()
```
```
// ... rest of initialization
}
}
```
**Step 2: Integrate Bypass into Per-File Processing**

The bypass system is integrated into the per-file analysis workflow:

```
// Modified per-file analysis method
private func analyzeFileWithSanitizationBypass( _ file:
FileInventoryItem) async throws -> FileAnalysisResult {
// Step 1: Extract content using existing methods
let extractedContent = try await extractFileContent(file)
```
```
// Step 2: Process content through sanitization bypass
let fileContext = FileContext(
fileName: file.fileName,
fileExtension: file.fileExtension,
directoryPath:
file.url.deletingLastPathComponent().path,
fileSize: file.fileSize,
hasRichMetadata: file.contentType != nil ,
audioMetadata: try? await extractAudioMetadata(file.url)
)
```
```
let safeContentResult = try await
sanitizationBypass.processContentSafely(
extractedContent,
fileContext: fileContext,
processingMode: .comprehensive
)
```
```
// Step 3: Handle different bypass results
switch safeContentResult.bypassReason {
case .requiresUserConfirmation:
return try await handleUserConfirmationRequired(file,
safeContentResult)
case .metadataFallback, .contextOnly:
return try await performAlternativeAnalysis(file,
safeContentResult)
default :
```

```
// Content is safe to process normally
guard let tokenEngine = tokenSafeEngine else {
throw OrganizationError.foundationModelsNotAvailable
}
```
```
return try await tokenEngine.analyzeFile(file, content:
safeContentResult.processedContent)
}
}
```
**Step 3: Handle User Confirmation Workflows**

For cases where the bypass system cannot automatically determine content safety,

implement user confirmation workflows:

```
private func handleUserConfirmationRequired( _ file:
FileInventoryItem, _ safeContentResult: SafeContentResult)
async throws -> FileAnalysisResult {
// Present user confirmation dialog
let userConfirmation = await presentUserConfirmationDialog(
fileName: file.fileName,
reason: "This filename contains content that requires
manual review",
options: ["Process as Technical Content", "Skip File",
"Use Metadata Only"]
)
```
```
switch userConfirmation {
case .processAsTechnical:
// User confirmed it's legitimate technical content
guard let tokenEngine = tokenSafeEngine else {
throw OrganizationError.foundationModelsNotAvailable
}
return try await tokenEngine.analyzeFile(file, content:
safeContentResult.processedContent)
```
```
case .skipFile:
// Create a skipped file result
return createSkippedFileResult(file, reason:
"Skipped by user request")
```
```
case .useMetadataOnly:
// Fall back to metadata-only analysis
return try await performMetadataOnlyAnalysis(file)
}
}
```
**Step 4: Update Progress Reporting**


Modify the progress reporting system to include sanitization bypass status:

```
// In the main organization loop
for (index, file) in supportedFiles.enumerated() {
let progress = 0.2 + (Double(index) / Double(totalFiles)) *
0.
```
```
// Update progress with sanitization status
let statusMessage: String
if file.fileName.containsPotentiallyProblematicContent {
statusMessage = "Analyzing \( file.fileName ) (checking
content safety)..."
} else {
statusMessage = "Analyzing \( file.fileName ) ..."
}
```
```
progressCallback(progress, statusMessage)
```
```
do {
let analysisResult = try await
analyzeFileWithSanitizationBypass(file)
analysisResults.append(analysisResult)
```
```
// Log bypass decisions for debugging
if let bypassReason = analysisResult.bypassReason {
print("File \( file.fileName ) processed with bypass
reason: \( bypassReason ) ")
}
```
```
} catch {
// Handle sanitization errors gracefully
let fallbackResult = createFallbackAnalysis( for : file,
error: error)
analysisResults.append(fallbackResult)
}
}
```
### Configuration and Customization

The sanitization bypass system supports various configuration options to adapt to

different use cases:

**Bypass Sensitivity Configuration:**

```
struct BypassConfiguration {
var confidenceThreshold: Double = 0.
var enableQuickPass: Bool = true
var enableUserConfirmation: Bool = true
var maxProcessingTime: TimeInterval = 0.
```

```
var auditLogging: Bool = true
```
```
// Audio-specific settings
var audioEquipmentWhitelistEnabled: Bool = true
var technicalSpecificationBypassEnabled: Bool = true
var contextualAnalysisEnabled: Bool = true
```
```
// Custom whitelist entries
var customWhitelistEntries: [String] = []
var customManufacturerNames: [String] = []
}
```
**User Interface Integration:**

The bypass system integrates with the existing user interface to provide transparency

and control:

```
// Add to FileSorterAppState
@Published var sanitizationBypassEnabled: Bool = true
@Published var bypassSensitivity: Double = 0.
@Published var showBypassDecisions: Bool = false
@Published var bypassStatistics: BypassStatistics =
BypassStatistics()
```
```
struct BypassStatistics {
var totalFilesProcessed: Int = 0
var filesBypassed: Int = 0
var filesRequiringConfirmation: Int = 0
var averageBypassConfidence: Double = 0.
}
```
**Settings Interface:**

```
// In SettingsView.swift
Section("Content Safety") {
Toggle("Enable Content Sanitization Bypass", isOn:
$appState.sanitizationBypassEnabled)
.help("Allows processing of legitimate technical content
that might otherwise be blocked")
```
```
if appState.sanitizationBypassEnabled {
VStack(alignment: .leading) {
HStack {
Text("Bypass Sensitivity")
Spacer()
Text(" \( Int ( appState.bypassSensitivity * 100 ))
%")
}
```

```
Slider(value: $appState.bypassSensitivity, in :
0.5...0.95, step: 0.05)
.help("Higher values require more confidence
before bypassing content filters")
}
```
```
Toggle("Show Bypass Decisions", isOn:
$appState.showBypassDecisions)
.help("Display information about content
sanitization bypass decisions")
}
}
```
### Error Handling and Fallback Strategies

The integration includes comprehensive error handling to ensure robust operation:

**Graceful Degradation:**

```
private func handleSanitizationBypassError( _ error: Error,
file: FileInventoryItem) async -> FileAnalysisResult {
print("Sanitization bypass failed for \( file.fileName ) : \
( error ) ")
```
```
// Attempt fallback strategies in order of preference
```
```
// Strategy 1: Metadata-only analysis
if file.contentType != nil {
do {
return try await performMetadataOnlyAnalysis(file)
} catch {
print("Metadata-only analysis failed: \( error ) ")
}
}
```
```
// Strategy 2: Algorithmic categorization
let algorithmicCategory =
algorithmicEngine.categorizeByFileType(file)
return FileAnalysisResult(
fileID: file.id,
fileName: file.fileName,
fileType: file.fileTypeCategory,
contentSummary: "Categorized using algorithmic fallback
due to content safety restrictions",
suggestedCategory: algorithmicCategory,
confidence: 0.6,
reasoning: "Content sanitization bypass failed, used
algorithmic categorization",
technicalMetadata: createBasicTechnicalMetadata(file),
categoryDecision: CategoryDecision(
```

```
category: algorithmicCategory,
confidence: 0.6,
reasoning: "Algorithmic fallback",
alternativeCategories: [],
organizationStrategy: .byType
)
)
}
```
**Monitoring and Diagnostics:**

```
class SanitizationBypassMonitor {
private var bypassDecisions: [BypassDecision] = []
```
```
func recordBypassDecision( _ decision: BypassDecision) {
bypassDecisions.append(decision)
```
```
// Log concerning patterns
if decision.confidence < 0.5 {
print("Low confidence bypass decision for \
( decision.fileName ) : \( decision.reason ) ")
}
```
```
// Update statistics
updateBypassStatistics(decision)
}
```
```
func generateBypassReport() -> BypassReport {
return BypassReport(
totalDecisions: bypassDecisions.count,
averageConfidence: bypassDecisions.map {
$0.confidence }.reduce( 0 , +) / Double(bypassDecisions.count),
bypassReasons: Dictionary(grouping:
bypassDecisions, by: { $0.reason }),
problematicFiles: bypassDecisions.filter {
$0.confidence < 0.5 }
)
}
}
```
This comprehensive testing framework and integration guide ensures that the content

sanitization bypass system can be effectively implemented and validated within the

FileSorter application, enabling reliable processing of legitimate technical content while

maintaining appropriate safety measures.